<strong>Subject > </strong> <p class ='subject-title' ><%= link_to @book.subject.title,
book_show_subjects_path(@subject) %><br /></p>

<h1><%= @book.title %></h1>

<p>
   <strong>Subject :</strong> <%= @book.subject.title %><br />
   <strong>Created Date:</strong> <%= @book.created_at.strftime("%d-%m-%Y") %><br />
   by  <%= current_user.username %>
</p>

<p><%= @book.body %></p>

<hr />

<%= link_to 'Back', {:action => 'list'} %>